# neovim configuration
My neovim configuration

## Installation
To install the configuration, use the following commands:
```bash
mkdir -p ~/.config/nvim
cd ~/.config/nvim
git clone git@github.com:simonzweers/nvim.git .
```
